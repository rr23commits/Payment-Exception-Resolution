services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: payment_engine
      POSTGRES_USER: payment_engine
      POSTGRES_PASSWORD: payment_engine
    ports:
      - "5432:5432"
